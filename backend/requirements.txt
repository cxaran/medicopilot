fastapi==0.138.0
uvicorn[standard]==0.49.0
gunicorn==26.0.0

sqlmodel==0.0.38
psycopg2-binary==2.9.12
alembic==1.18.4

pydantic[email]==2.13.4
pydantic-settings==2.14.2

fastapi-mail==1.6.5
python-multipart==0.0.32
redis==8.0.0

# Cliente HTTP para el intercambio/refresh de tokens OAuth con el proveedor (B10).
httpx==0.28.1

# Base IANA de zonas horarias para zoneinfo (semántica de calendario de filtros de
# fecha). Garantiza disponibilidad en Windows y en la imagen slim, sin depender del
# tzdata del sistema.
tzdata==2026.2

argon2-cffi==25.1.0

# Elige una estrategia JWT principal:
PyJWT[crypto]==2.13.0
# python-jose[cryptography]==3.5.0

# Cifrado en reposo de secretos de credenciales de proveedor de IA (Fernet).
cryptography==49.0.0

# Mantener solo mientras existan hashes bcrypt heredados:
passlib[bcrypt]==1.7.4

# Extracción de TEXTO de PDFs de documentos clínicos (F-MEDIOS fase 1). Pura-python, sin
# OCR ni SDKs de nube: solo lee la capa de texto del PDF para que el AGENTE interprete los
# analitos. Los PDFs escaneados (sin capa de texto) quedan para fase 2 (OCR).
pypdf==6.14.2